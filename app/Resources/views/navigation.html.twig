<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#nav-main" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">
                <img alt="herrwelter" src="/img/icons/herrwelter_icon.png">
            </a>
        </div>

        <div class="collapse navbar-collapse" id="nav-main">
            <ul class="nav navbar-nav">
                <li><a href="/">herrwelter</a></li>
                {% if not isBackend %}
                    <li><a href="/#about">Wer ist herrwelter?</a></li>
                    <li><a href="/contractor">Für Dienstleister</a></li>
                {% endif %}
            </ul>
            {% if not isBackend %}
                <form class="navbar-form navbar-left" role="search">
                    <div class="form-group">
                        <input type="text" class="form-control" placeholder="Suchbegriff">
                    </div>
                    <button type="submit" class="btn btn-orange">Suche</button>
                </form>
            {% endif %}
            <ul class="nav navbar-nav navbar-right">
                {% if app.user %}
                    <li><a href="/logout">Abmelden</a></li>
                {% else %}
                    <li><a href="/provider/register">Registrieren</a></li>
                    <li><a href="/login">Login</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>